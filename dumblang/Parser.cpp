#include "Parser.h"
#include "AstNodes.h"
#include "Token.h"

void ParseTokens(std::vector<Token> stream) {
	for (Token& token : stream)
	{
		switch (token.type){
		case Token::NUM:
			
		default:
			break;
		}
	}
}
#include "Parser.h"
#include "Token.h"
#include "Interpreter.h"

void ParseTokens(std::vector<Token> stream) {
	for (Token& token : stream)
	{
		switch (token.type){
		case Token::NUM:
			
		default:
			break;
		}
	}
}
